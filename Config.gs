/**
 * ============================================================
 * Avery Label Generator — Config.gs
 * ============================================================
 *
 * Edit these constants to match your Google Sheet layout.
 * Label format is chosen at runtime from the Avery Labels menu.
 * ============================================================
 */

const LABEL_COLUMN = "B";    // Column containing label text
const LABEL_START_ROW = 2;   // First row with data (row 1 assumed to be a header)

