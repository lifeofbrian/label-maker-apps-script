/**
 * ============================================================
 * Configuration for Avery Label Generator
 * ============================================================
 */

// Which column contains the label text (A, B, C, etc.)
const LABEL_COLUMN = "B";

// First row with label data (row 1 = headers, so start at 2)
const LABEL_START_ROW = 2;

// Default label type to preselect in the dialog
const LABEL_TYPE = "5160";
